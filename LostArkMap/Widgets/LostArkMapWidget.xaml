<Page
    x:Class="LostArkMap.Widgets.LostArkMapWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameBarUtils.Widgets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid x:Name="BackgroundGrid" Grid.Row="0" Grid.RowSpan="2"/>     
                
        <WebView x:Name="webView" Grid.Row="0" Grid.RowSpan="2"/>

        <Grid Grid.Row="0">
            <StackPanel>
                <Button Click="HomeButton_Clicked" HorizontalAlignment="Center" ToolTipService.ToolTip="Home">
                    <SymbolIcon Symbol="Home"/>
                </Button>                
            </StackPanel>
        </Grid>
    </Grid>
</Page>
